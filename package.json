{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "devDependencies": {
    "nuxt": "^3.3.3",
    "push-dir": "^0.4.1"
  },
  "dependencies": {
    "firebase": "^9.19.1",
    "sass": "^1.61.0",
    "vuex": "^4.0.2"
  },
  "homepage": "https://github.com/roh96/nuxtportfolio/"
}
